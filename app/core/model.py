"""PLACEHOLDER.

Replace this file with model.py from your repository, unchanged.

It must define the class named by MODEL_CLASS in predictor.py, currently
"GATPortfolio", containing the BiGRU temporal encoder, the GATConv layers,
and the portfolio head.

Do not modify your model code to fit this platform. If the calling
convention differs, change _forward() in predictor.py instead. Keeping
your research code untouched is what lets you retrain locally and deploy
without a translation step.
"""

raise ImportError(
    "core/model.py is a placeholder. Copy your real model.py over it."
)
